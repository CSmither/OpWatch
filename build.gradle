subprojects {
    apply plugin: 'java'
    apply plugin: 'idea'
    group = 'org.smither.opwatch'
    version = '1.0'
    repositories {
        mavenCentral()
    }
    sourceCompatibility = 1.8
    targetCompatibility = 1.8
}

project(':server') {
    dependencies {
        compile project(':utils')
    }
}



project(':spigot') {
    dependencies {
        compile project(':utils')
    }
}